# public/ — required assets

- **`resume.pdf`** — your CV. Linked from the nav `cv` button and the Contact section ("download cv"). Until this file exists, those links 404.

Filename must match exactly (case-sensitive). No code changes needed once dropped in.
